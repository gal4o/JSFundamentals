function solve(num) {
	let sum = 0;
	let isSame = true;
	let n = num.toString();
	for(let i = n.length-1; i>=0; i--) {
		sum+=Number(n[i]);
		if (i!==0 && (n[i]!== n[i-1])){
		isSame = false;
		}
	}
	console.log(isSame);
	console.log(sum);
}
