function solve(num1, num2) {
	let result;
	let maxNum = Math.max(Number(num1), Number(num2));
	let minNum = Math.min(Number(num1), Number(num2));
	for(let i = minNum; i>=1; i--) {
		if(maxNum%i === 0 && minNum%i === 0) {
			console.log(i);
			break;
		}
	}
}
